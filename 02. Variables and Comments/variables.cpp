/* C and C++ are low level languages as we can closely interact with the computer hardware using these languages.*/
/* A variable must be declared first before using it and must be declared with a datatype*/
#include<iostream>

using namespace std;
int main() {
    int sum = 5;
    string name = " Rishil";
    std:cout<< "Hello World " << sum << name;
    return 0;
}